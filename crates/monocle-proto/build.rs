//! monocle-proto build script.
//!
//! No-op stub created in S-001 — no `.proto` files in Phase 1. Populated by S-013
//! with `prost_build::Config::new().compile_protos(...)` call producing generated
//! types from `proto/hook_event.proto`.

fn main() {
    // No `.proto` files to compile in Phase 1. S-013 will replace this.
}
