[package]
name = "monocle-proto"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Protobuf-encoded wire format for monocle hook events (activated by S-013 + Phase 4 federation)."

[lints]
workspace = true

[dependencies]
prost = { workspace = true }
bytes = { workspace = true }

[build-dependencies]
prost-build = { workspace = true }

[dev-dependencies]
# prost-types: FileDescriptorSet deserialization for VP-016 / BC-2.02.006 wire_field_order.rs test.
# Exact pin "=0.14.1" matches prost-types' own minimum prost requirement (^0.14.1) and aligns
# with the workspace's exact prost pin "=0.14.1" — no version conflict.
# Declared as crate-level dev-dep (not workspace) per least-privilege policy for test-only crates
# (see SS-deps-pin-manifest.md v1.2.0 §Dev Dependencies).
prost-types = "=0.14.1"
